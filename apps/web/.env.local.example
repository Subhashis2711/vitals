NEXT_PUBLIC_API_URL=http://localhost:4000

# --- Supabase (safe to expose — these are the public, anon-scoped keys) ---
# Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
