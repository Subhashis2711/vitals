{
  "name": "vitals",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w packages/shared && npm run build -w packages/ai && npm run build -w packages/db && npm run build -w apps/api && npm run build -w apps/web",
    "db:generate": "dotenv -e .env -- npm run generate -w packages/db",
    "db:migrate": "dotenv -e .env -- npm run migrate -w packages/db",
    "db:seed": "dotenv -e .env -- npm run seed -w packages/db",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w packages/ai && npm run typecheck -w packages/db && npm run typecheck -w apps/api && npm run typecheck -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "dotenv-cli": "^8.0.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
